<Page
    x:Class="AIOneUWPCX.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIOneUWPCX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	d:DesignHeight="484" d:DesignWidth="1152">
	<Page.Background>
		<AcrylicBrush BackgroundSource="HostBackdrop"
                      TintColor="Black"
                      TintOpacity="0.6"
                      FallbackColor="Transparent"/>
	</Page.Background>

	<Page.Resources>
		<local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
	</Page.Resources>

	<Grid Margin="10">
		<!--<Grid.Background>
			<AcrylicBrush BackgroundSource="HostBackdrop"
                          TintColor="Black"
                          TintOpacity="0.6"
                          FallbackColor="Transparent"/>
		</Grid.Background>-->
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="10"/>
			<RowDefinition/>
			<RowDefinition Height="20"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Button HorizontalAlignment="Stretch" Click="Button_Click">Load model</Button>
		<ProgressBar x:Name="ModelProgressBar" Grid.Row="1" Value="0" Maximum="100"/>
		<ListView Grid.Row="3" ItemsSource="{x:Bind Messages, Mode=OneWay}" SelectionChanged="ListView_SelectionChanged">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Message">
					<StackPanel>
						<TextBlock Text="{x:Bind Role, Mode=OneWay}" Margin="0,5,5,0" FontWeight="Bold" FontSize="14" />
						<ToggleButton x:Name="ExpandButton" FontWeight="SemiBold" Background="Transparent" Visibility="{x:Bind ReasoningVisibility, Mode=OneWay}">
							<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
								<TextBlock Text="▶ Reasoning" Margin="0,0,5,0"/>
								<ProgressRing Width="16" Height="16" IsActive="{x:Bind Reasoning, Mode=OneWay}"/>
							</StackPanel>
						</ToggleButton>
						<StackPanel Margin="10,0,0,0" Visibility="{Binding IsChecked, ElementName=ExpandButton, Converter={StaticResource BoolToVisibility}}">
							<TextBlock Text="{x:Bind Thoughts, Mode=OneWay}" TextWrapping="Wrap"/>
						</StackPanel>
						<TextBlock Text="{x:Bind Text, Mode=OneWay}" Margin="5,0,5,5" TextWrapping="Wrap"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<Grid Grid.Row="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="10"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<TextBox x:Name="MessageInput" Grid.Column="0"></TextBox>
			<Button Grid.Column="2" Click="Button_Click_1">Send</Button>
		</Grid>
	</Grid>
</Page>
