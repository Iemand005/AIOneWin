<Page
    x:Class="AIOneUWPCX.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIOneUWPCX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DesignHeight="484" d:DesignWidth="1152">

	<Grid Margin="10">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="10"/>
			<RowDefinition/>
			<RowDefinition Height="20"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Button HorizontalAlignment="Stretch" Click="Button_Click">Load model</Button>
		<ProgressBar x:Name="ModelProgressBar" Grid.Row="1" Value="0" Maximum="100"/>
		<ListView Grid.Row="3" ItemsSource="{x:Bind Messages, Mode=OneWay}" SelectionChanged="ListView_SelectionChanged">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Message">
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{x:Bind Role, Mode=OneWay}" Margin="5"/>
						<TextBlock Text="{x:Bind Text, Mode=OneWay}" Margin="5"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<Grid Grid.Row="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="10"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<TextBox x:Name="MessageInput" Grid.Column="0"></TextBox>
			<Button Grid.Column="2" Click="Button_Click_1">Send</Button>
		</Grid>
	</Grid>
</Page>
